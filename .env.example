# Copy to .env and fill in before `docker compose up -d`.
# A hardcoded/auto-generated default key was deliberately avoided here -
# an API key everyone starts with is not a real secret.
INDIC_TTS_API_KEY=changeme-generate-with-openssl-rand-base64-32
XLIT_API_KEY=
